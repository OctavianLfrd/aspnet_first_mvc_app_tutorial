@{ 
    ViewData["Title"] = "Setting View Data";
}
<h2>Let's set the view data</h2>
<ul>
    @for (int i = 0; i < (int) ViewData["NumTimes"]; i++)
    {
        <li>@ViewData["Message"]</li>
    }
</ul>